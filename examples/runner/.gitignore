# Generated by examples/runner/build-fixtures.mjs — regenerate with
#   node examples/runner/build-fixtures.mjs
# Kept out of git so the tree stays text-only and so we never accidentally
# pin a stale binary fixture to a non-matching schema version.
clean.tar.gz
regression.tar.gz
cross-runtime-diff-clean.json
cross-runtime-diff-regression.json
