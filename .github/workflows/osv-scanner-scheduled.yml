name: Scheduled OSV Advisory

on:
  schedule:
    - cron: "59 5 * * 1"
  workflow_dispatch:

permissions: {}

jobs:
  osv-npm:
    name: OSV npm advisory scan
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Run OSV scanner
        uses: google/osv-scanner-action/osv-scanner-action@9fd1bcce27f67e3bd819a0a7620e332803dc43bc # v2.3.8
        with:
          scan-args: |-
            --format=json
            --output=osv-results.json
            harness/package-lock.json
        continue-on-error: true

      - name: Build advisory SARIF
        uses: google/osv-scanner-action/osv-reporter-action@9fd1bcce27f67e3bd819a0a7620e332803dc43bc # v2.3.8
        with:
          scan-args: |-
            --output=osv-results.sarif
            --new=osv-results.json
            --gh-annotations=false
            --fail-on-vuln=false

      - name: Upload SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@e4fba868fa4b1b91e1fdab776edc8cfbe6e9fb81 # v4.37.3
        with:
          sarif_file: osv-results.sarif
          category: osv-scanner-npm
